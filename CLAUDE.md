# Bagpipe Lab

Bagpipe/practice-chanter learning app. Part of the Highland Music, LLC portfolio — see `pablohoney1977/highland-music-site` (`CLAUDE.md`) for business/legal status and shared infrastructure decisions (RevenueCat one-time IAP, PostHog, Codemagic, Capacitor, audio sample sourcing) that this app inherits by default unless noted otherwise here.

## Concept

Interactive, animated practice-chanter trainer. Core differentiator vs. existing bagpipe learning material (tutor-book PDFs, YouTube channels, static fingering/metronome apps): none of them combine an animated interactive chanter with a rhythm-driven play-along loop.

**Why rhythm/timing, not pitch matching:** chanter fingering is deterministic — one fingering produces exactly one pitch, no embouchure or pitch-bending. So unlike pitch-matching apps, the skill worth grading is timing and finger-sequence accuracy. Core gameplay loop: animated SVG chanter with holes lighting up in sequence, played against a falling-notes/rhythm-lane UI synced to a backing play-along track.

**Why embellishments early but not too early:** a chanter has no tonguing and can't silence or restart a note — it's one continuous reed tone. Grace-note embellishments (starting with the doubling) aren't decoration; they're the only way to separate two repeated notes rhythmically. Introduce the doubling right after the student can hold steady rhythm on an unembellished simple tune, framed as solving a problem they've already felt (their repeated notes blur together) — not as an early flourish or a late afterthought.

## Curriculum phases

1. **Meet the chanter** — interactive hole diagram (8 holes, hand position), tap-to-hear each of the 9 notes (Low G, Low A, B, C, D, E, F, High G, High A).
2. **The scale** — full octave play-along at slow tempo, visual finger-highlight leads eye/ear, ascending and descending.
3. **Steady rhythm, no embellishments** — simple traditional (public-domain) melodies, scored purely on timing/finger-sequence accuracy. Checkpoint here gates phase 4.
4. **The doubling** — introduced with the functional "why," practiced on the repeated-note passages from phase 3's tunes.
5. **First real tunes** combining scale + doubling.
6. **Progressive embellishments** (strike, grip, throw-on-D, taorluath, birl) — each introduced in the specific tune context that requires it, each gated behind a rhythm-accuracy checkpoint.

## Monetization

Free tier: phases 1–3 (scale + rhythm fundamentals). Paid one-time unlock: phases 4–6 (tunes + embellishments) — mirrors Jazz Guitar Lab's freemium boundary, one-time IAP per portfolio policy (no subscriptions).

## Market note

Smaller, more niche audience than guitar/piano — lower download ceiling, less competition, more committed/willing-to-pay audience. Organic marketing via piping-specific communities (r/bagpipes, pipe band forums) rather than general music-app ASO.

## Roadmap — deferred until fundamentals ship

- **Mic-based rhythm detection.** Longer-term goal: use the device mic to detect actual note-onset timing from a real practice chanter, instead of tap/metronome-sync input, so scoring reflects real playing rather than tapping a screen in rhythm.
  - Framing: a chanter's reed tone is harmonically rich/noisy compared to a clean metronome click, but *onset detection* (when a note attacks) is a different and generally easier problem than pitch detection — we don't need to identify which note was played (fingering already tells us that), just when a transition happened. This should make it more tractable than it first sounds.
  - Sequencing: build and ship the tap/metronome-sync version first (phases 1–6 above), validate the curriculum and retention with that, then layer mic-based onset detection in as an upgrade to the existing rhythm-accuracy scoring — not a blocker to first release.
  - Open questions to resolve when this gets picked up: onset-detection approach/library (e.g. spectral flux or energy-based onset detection vs. a full pitch tracker misapplied to this problem), mic permission/UX on iOS, noise/environment robustness, latency budget for real-time scoring.

## Tech stack (inherited from portfolio pattern unless noted)

- Single-file React PWA + Capacitor (`ios/` project committed to this repo)
- RevenueCat (`@revenuecat/purchases-capacitor`) for the one-time IAP unlock
- PostHog analytics (CDN snippet, no-op until a real project key is set)
- Codemagic CI/CD (`codemagic.yaml`)
- Audio: recorded practice-chanter/pipe samples where available (check `nbrosowsky/tonejs-instruments` first), synthesized reed/drone tone as fallback for any note or instrument mode without a clean recorded sample — hybrid, not an either/or.
- Instrument modes: practice chanter (primary, drives the whole curriculum) and full Highland pipes (same fingering logic, adds drone audio) both supported.
- Bundle ID on the `com.pablohoney.*` namespace

## Decisions

- **Instruments:** support both practice chanter and full Highland pipes (drone + chanter, same fingering). Chanter remains the primary/default mode the curriculum is built around.
- **Audio sourcing:** hybrid — recorded samples where available, synthesized tone filling gaps (missing notes, drone, or either instrument mode not yet sampled).
- **Repertoire:** public-domain tunes only for initial release. No tune is added to the shipped repertoire without its public-domain status confirmed first.

## Open questions

- None blocking phase 1 as of this update — audio sourcing, instrument scope, and repertoire policy are resolved above. Revisit if a specific tune's public-domain status is ambiguous, or if sample sourcing for a given note/instrument proves impractical.

## Working Memory
> Rolling snapshot, overwrite don't append.

- **Repo created (2026-07-12):** `bagpipe-lab` created by the user directly (the ops-hub session's GitHub integration isn't authorized to create repos — 403 — same constraint noted in `highland-music-site`'s `CLAUDE.md`). This `CLAUDE.md` seeded from the strategy work done in that ops-hub session/`bagpipe-app-strategy.md` before the ops session's repo-adding connector got stuck, so a fresh session here starts with full context instead of none.
- **App shell scaffolded (2026-07-12):** Vite + React + TS PWA in repo root, single-file-style `src/App.tsx` holding a placeholder for phase 1 ("Meet the chanter"). Capacitor added (`@capacitor/core`, `@capacitor/cli`, `@capacitor/ios`), `ios/` project generated and committed, bundle id `com.pablohoney.bagpipelab`. `codemagic.yaml` added mirroring the portfolio's Capacitor iOS build pattern. App Store checklist section added to README. Merged to `main` via PR #1.
- **Open questions resolved (2026-07-12):** both instruments (chanter + full pipes) in scope; audio sourcing is hybrid (recorded samples + synthesized fallback); repertoire starts public-domain-only. Merged via PR #2.
- **Phase 1 built (2026-07-12):** interactive chanter trainer live in `src/App.tsx` — SVG hole diagram (`src/ChanterDiagram.tsx`) grouped by top/bottom hand, 9-note picker (`src/chanter.ts`) with the standard cumulative fingering chart, tap-to-hear via a synthesized Web Audio tone (placeholder pending recorded samples). Verified working in-browser with Playwright, both themes. Merged via PR #3.
- **Chanter sample sourcing researched, no source found (2026-07-12):** checked `nbrosowsky/tonejs-instruments` (19 instruments, no reed/bagpipe samples), Wikimedia Commons, Freesound, KVR/VI-Control forums, GitHub bagpipe/soundfont projects, Pixabay/Sample Focus/Splice/Musical Artifacts. No clean, redistribution-safe library of isolated chanter notes (Low G–High A) exists in the free/CC ecosystem — bagpipes are sampled as loops/drones/one-shots, not as a chromatic per-note instrument like piano/violin. Commercial loop libraries' "royalty-free" licenses are typically for use *in music tracks*, not for redistributing the raw audio as an app asset — a distinction worth re-checking carefully if a paid source is considered later. **Decision: keep the synthesized placeholder tone for launch.** Revisit real samples by recording the 9 notes directly (cleanest rights path) if/when that becomes practical, or if a vetted licensed source turns up.
- **Phase 2 built (2026-07-12):** scale play-along live in `src/TheScale.tsx` — steps through the full octave ascending then descending (17 notes, Low G→High A→Low G) at a slow fixed tempo (550ms/note), chanter diagram and note name follow the current step, progress dots and an ascending/descending label track position. Added simple phase-nav tabs in `src/App.tsx`; phase 1 extracted to `src/MeetTheChanter.tsx`. Verified working in-browser with Playwright — tab switch, full play-through, auto-stop/reset at the end, no console errors.
- **Chanter diagram corrected (2026-07-12):** the original diagram modeled 8 front holes with no thumb hole, which is wrong — a real Highland/practice chanter has 7 front holes + 1 thumb hole on the back. Verified the correct 8-hole layout and full 9-note fingering chart against multiple sources (this also caught that High G's fingering was wrong before: it's not a simple continuation of the low-note pattern — it re-covers R1–R3 while opening the top hand except the thumb, due to overblowing). `src/chanter.ts` and `src/ChanterDiagram.tsx` rebuilt accordingly: thumb hole rendered separately (dashed outline, "back" label, leader line) from the 7 front holes, plus a more polished chanter silhouette (reed cap, turning rings, flared bell) instead of a plain rounded rectangle. Verified fingering correctness and visuals in-browser with Playwright, both themes.
- **Live preview:** deployed to GitHub Pages at https://pablohoney1977.github.io/Bagpipe-Lab/ via `.github/workflows/pages.yml` (auto-deploys on push to `main`). Repo was made public to enable Pages. Note: this session's sandbox blocks outbound `*.github.io`, so the live URL can't be fetched from here — rely on the Actions run `conclusion: success` as the deploy signal.
- **Workflow: merge my own PRs to `main` (2026-07-16):** the site is a solo preview (only the owner views it), and the owner explicitly asked me to merge changes to `main` myself, frequently, so work goes live — rather than leaving manual merges to them. So the standard flow is: commit → open PR → **I merge it to `main` myself** (squash), then report what was merged. Pages redeploys on the merge. Because the Pages `on: push: [main]` deploy only fires from `main`, work sitting on an unmerged branch never appears on the live site (this is what "I'm not seeing the changes" meant on 2026-07-16 — the PR simply wasn't merged yet).
- **Reframed as a full course (2026-07-12):** per user direction, the app is no longer a few flat interactive widgets — it's now a structured, beginning-to-end course ("square one → playing the Highland pipes"), modeled on the Jazz Guitar Lab tutorial depth. New architecture: `src/curriculum.tsx` (units → lessons data), `src/App.tsx` reworked into a contents/home view + lesson viewer with prev/next and localStorage completion tracking, `src/ui.tsx` (Callout/Steps/Figure/FactList/ComingSoon presentational components), `src/content.tsx` (real, source-verified instructional prose), `src/AnatomyDiagram.tsx` (labeled chanter + a double-reed close-up), `src/FingerMapDiagram.tsx` (which finger → which hole). Six units: Fundamentals (Welcome, anatomy, the reed, blowing steadily, holding, finger placement — all written), The nine notes (existing `MeetTheChanter`), The scale (existing `TheScale`), Steady rhythm (coming-soon stub), Embellishments (coming-soon stub), Onto the pipes (written preview). Instructional facts (left-hand-on-top, finger pads/sealing, double-reed mechanics, steady blowing) checked against real piping sources. Verified in-browser with Playwright — navigation, completion tracking, both themes, no console errors.
- **Model default:** `.claude/settings.json` sets the project default model to `claude-opus-4-8` (committed, applies repo-wide).
- **Fingering chart corrected, then verified against a user-provided chart (2026-07-12):** the earlier `chanter.ts` used a wrong recorder-style "lift one hole at a time" scale. The user first corrected the rules verbally (E-and-above keep low-hand R1–R3 down; C/D keep the pinky down), then sent an actual fingering chart image. Transcribed it to `reference/fingering-chart.md` (now the ground-truth source) and matched `src/chanter.ts` to it. Final grid resolved the cells I'd guessed: **C** opens R2+R3 (pinky planted), **D** opens R1+R2+R3 (pinky planted), **High A** = thumb+L1+L2 open but L3 (top ring) back down with the low hand R1–R3 covered (not all-open). NOTE: the provided chart was labelled "Scottish small pipe" — the 9-note fingering is standard-shared with the GHB practice chanter, but the SSP-vs-GHB equivalence (esp. High A) still wants an explicit thumbs-up from the user (task open). Piping sites all 403 the sandbox, so user-provided charts are the reliable path.
- **Reference ground-truth folder (2026-07-12):** started `reference/` to operationalize the "authoritative sources → repo → my ground truth" design-partnership mechanism. `reference/fingering-chart.md` (verified fingering grid) and `reference/embellishments.md` (embellishment vocabulary + the "timing tree" taxonomy — ends-on/starts-on/across the beat — transcribed from a Matt Willis / mattpiper.com infographic the user provided; image NOT committed, only the factual taxonomy). These are the canonical sources features should build against.
- **Hole visibility fixed (2026-07-12):** open holes were near-black on dark-brown wood = invisible (user reported "can't see the holes"). Added `--hole-rim` (light parchment) stroke on all holes so open holes read as hollow rings and covered holes as gold discs, in both themes; also lightened dark-theme `--wood` for contrast. Affects `.hole`, `.an-hole`, `.fm-hole` in `index.css`.
- **Restructured into a bottom tab-bar app (2026-07-12):** per user request to mimic their Jazz Guitar app's layout, `src/App.tsx` now has a fixed bottom tab bar with four tabs — **Guide** (the written course: Fundamentals + Onto the pipes, via `GUIDE_UNITS` in `curriculum.tsx`, with contents list + lesson viewer + localStorage read-tracking), **Scale** (`TheScale`), **Play** (`MeetTheChanter` note explorer + a "play-along tunes coming soon" note), **Grace notes** (embellishments, coming-soon). Inline SVG tab icons. `curriculum.tsx` trimmed to just the guide units (interactive pieces are now tabs, not lessons). Verified in-browser with Playwright at phone width, both themes, fingering grid dumped and checked, no console errors.
- **Design partnership + `PEDAGOGY.md` (2026-07-12):** the user asked me to be a genuine design partner (GHB + music-education expertise), not just an executor. Agreed mechanism: user supplies authoritative materials → I transcribe them into `reference/` as ground truth → features build against them. Drafted `PEDAGOGY.md` (co-owned design spine): design principles (mastery gating, tempo laddering, chunking, interleaving, problem-first ornaments, canntaireachd/sing-before-you-play, error-driven micro-drills), the stage-by-stage learning progression with concrete mastery criteria, the embellishment curriculum built on the timing-tree taxonomy (ends-on/starts-on/across the beat), a fault→drill catalogue, and a scoring model. Domain-specific parts are tagged `[needs owner]` for the piper to correct; §7 lists open questions for them.
- **Play rhythm engine v1 built (2026-07-12):** the flagship feature. `src/RhythmLane.tsx` — a canvas falling-notes lane synced to a Web Audio metronome (`playClick` added to `chanter.ts`), tap-to-hit (lane / Tap button / spacebar) judged by timing windows (perfect ≤100ms, good ≤200ms, miss if a note passes ~220ms unjudged), live perfect/good/miss counts + accuracy %, and the `ChanterDiagram` shows the upcoming note's fingering as you play. `src/tunes.ts` holds the `Exercise` model + four **original** scale-based practice patterns (Steady Low A, Low A & B, the scale in time, scale up-and-down) — deliberately not real tunes, so zero copyright question until the repertoire is chosen. Play tab now has a "Play along" / "Explore notes" segmented toggle + exercise picker; the note explorer (`MeetTheChanter`) moved under "Explore notes". Verified in-browser with Playwright: notes fall to the hit line, taps score, misses register, accuracy computes, game completes/resets, both themes, no console errors.
- **Owner answers on design (2026-07-12):** (a) High A on the GHB practice chanter **confirmed** identical to the smallpipe chart — fingering grid in `reference/fingering-chart.md` is now final (task #20 closed). (b) First tune = **Amazing Grace**, confirmed public-domain (Newton's 1779 words + the 1830s "New Britain" melody; only modern arrangements/recordings are protected, not the tune, so encoding the melody as note data is fine). (c) **Curriculum refinement — ornament philosophy:** tutors introduce embellishments too early; the owner's approach is **tune plain first → add simple single grace notes → then increasingly complex embellishments, layered onto already-known tunes.** This supersedes the "phase 4 = doubling first" framing in the Curriculum-phases section above — single grace notes come before the doubling now. First ornaments taught on Amazing Grace: high-G grace notes → D strike → light D throw → G catch. Captured in `PEDAGOGY.md` §4.
- **Amazing Grace draft setting encoded (2026-07-12):** added a proposed melody-only setting of Amazing Grace to `tunes.ts` (`AMAZING_GRACE`, shows as "Amazing Grace (draft)" in the Play picker, 3/4 @ 50bpm). Key arrangement decision: **set with D as the tonic** so the melody fits the chanter's Low A→High A range (pickup on Low A, the "I once was lost" high phrase on High A) — a straight transcription won't fit because the chanter floor is Low G. Notes used: Low A, B, D, E, F, High A (roughly pentatonic). Plays through the existing rhythm engine (`beatsPerBar` already supported). **DRAFT pending the owner's correction** of exact notes/rhythm — proposed note list was given to them in chat. No grace notes yet (tunes learned plain first).
- **Tempo laddering + mastery gating built (2026-07-12):** `src/RhythmLane.tsx` now holds an internal `bpm` state (starts at the exercise's bpm). A finished run computes accuracy; if it's ≥ `MASTERY_PCT` (85%) the "Tap" button is replaced by a green "Step up to N bpm ›" button that bumps the tempo by `TEMPO_STEP` (6 bpm, capped at 120) and resets for another go. Best accuracy per exercise persists in `localStorage` ('bagpipe-lab-best') and shows as a "Best X%" badge next to a tempo badge and the "Reach 85% to speed up" target; a run-result line reports mastered/try-again. This is `PEDAGOGY.md` §3's slow-first tempo ladder made real. Verified in-browser with Playwright: an accurate run hit 88%, showed "Mastered at 60 bpm", recorded best, and Step-up moved 60→66 bpm; no console errors.
- **Amazing Grace sheet received but NOT transcribed (2026-07-12):** the owner sent a scanned pipe setting of Amazing Grace (9/8 time, with the embellishments already written in — high-G gracenotes, doublings, throw on D, a birl). Deliberately did NOT transcribe it note-by-note: the scan resolution + grace-note density make exact staff-position reading unreliable, and guessing would repeat the fingering-error pattern we've worked to avoid. The `AMAZING_GRACE` draft in `tunes.ts` (3/4, D-tonic) still stands as a best-effort of the known melody. NEXT: get the plain melody as note names from the owner (or a clearer per-line photo) to finalize; the sheet's ornaments are the reference for the LATER grace-note stage (learn plain first).
- **"Reading the music" unit added to the Guide (2026-07-13):** per user direction, the Guide needed to actually teach music reading (not just fingering) and to end at "ready for the pipes," pointing to the other tabs for practice rather than trying to be the practice surface itself. Added a new unit between Fundamentals and Onto the pipes with three lessons: "Why bagpipe notation is simple" (fixed-pitch instrument → no key signature, no accidentals, one clef, one ~2-octave range), "Reading the rhythm" (note durations, bars/time signatures, common tune-type time signatures, tied explicitly to the Play tab's bpm/beatsPerBar model), and "The staff, note by note" (`src/StaffDiagram.tsx`, a from-scratch SVG staff renderer using a half-line-spacing "step" system with correct ledger-line logic). Welcome()'s course-path FactList updated to include the new stage.
- **Staff-position ground truth verified by pixel measurement, not memory (2026-07-13):** initially asked the owner to confirm which of two remembered conventions was right (bottom line = Low A vs. one line up); owner instead sent `FingeringChart1.png` (a "Bagpipe Scale" chart) to transcribe, same mechanism as the fingering chart. External piping/notation sites are blocked by this session's egress policy (confirmed via the agent-proxy status endpoint — org policy 403, not a fixable tool issue), so verified by cropping and pixel-analyzing the image directly (Python/PIL: measured the 5 staff-line y-positions and each note's filled-notehead centroid) rather than trusting recalled convention. Result, documented in `reference/staff-notation.md`: Low G sits on the **2nd line from the bottom** (the staff's actual bottom line is unused by any chanter note) — not hanging below the staff as common shorthand suggests. Low A→F then climb one line/space at a time up to F on the top line; High G floats in the space just above the staff with no ledger; High A alone sits on a true ledger line above the staff. This is now `src/StaffDiagram.tsx`'s `NOTE_STEPS` table.
- **Guide reworked into a coaching PATH; Play gained a "Read the music" mode (2026-07-16):** the user's critique — the Guide had become "a bunch of words that condenses many months into 5 minutes," disconnected from the practice tabs, and Play was only a rhythm/timing game with no way to read real notation. Modeled the fix on the **Jazz Guitar app** (`pablohoney1977/jazz-guitar-app`, cloned to `/workspace/jazz-guitar-app` for reference — its Guide is a path of stage cards, each: short concept → a button that opens the right tool tab **preconfigured** via `openPreset()` → a practice checklist → a mastery gate, with deep prose behind a "Why it works" expander). Changes: (a) **Guide is now a path** — `src/curriculum.tsx` exports `STAGES` (15 stages across 5 phases) instead of units/lessons; `GuideTab`/`StageCard` in `App.tsx` render collapsible stage cards with per-item checkbox tracking (`bagpipe-lab-checklist`) + stage-done tracking (`bagpipe-lab-progress`); the old prose components in `content.tsx` are now the `why()` deep-dives. `Welcome()` rewritten to explain how to USE the app (Guide = map, tabs = practice room). (b) **`openPreset` navigation** — active tab + Play exercise/mode lifted into `App`; nav types in new `src/nav.ts` (avoids an App↔curriculum cycle); stage CTAs jump straight into Scale / Play(feel|read|notes) preconfigured. (c) **"Read the music" mode** — new `src/StaffPlayer.tsx` renders an exercise as real bagpipe staff notation (noteheads at the verified `staff.ts` positions + bar lines + stems, count-in display) and sweeps a playhead in time, sounding + highlighting each note and following the fingering diagram; it's a reading play-along, not scored (the falling-notes lane keeps that job). Play tab now has three modes: Feel the pulse / Read the music / Explore notes. (d) **Shared modules extracted** so the lane and staff agree: `src/staff.ts` (note→staff geometry, used by StaffDiagram + StaffPlayer) and `src/rhythmEngine.ts` (`buildTimedExercise` timing schedule, used by RhythmLane + StaffPlayer). Caught a real bug during review — StaffPlayer read `n.note` (undefined on `TimedNote`, field is `n.name`) so every notehead silently fell to the middle line; esbuild dev doesn't type-check so it only surfaced in `tsc -b`. Verified in browser (Playwright): Guide path + CTA routing (confirmed tab+mode+exercise land preconfigured), Feel lane still scores after the refactor, Read mode plays with correct pixel-measured note heights (Low G cy=100 → High A cy=20) and the playhead runs to the final bar, both themes, no console errors.
- **Paper tutor reviewed + "Finger Gym" built (2026-07-16):** the owner photographed a comprehensive paper tutor — **Lloyd M. Bogart, _Bagpipes: The Manual_ (© 1990, La Crosse, WI)** — 123 pages, HyperCard-era. Its structure: fundamentals → **Triad Exercises** (Sections 2–4: right-hand / left-hand / two-handed note-to-note drills) → embellishments (Sections 5–7: gracenotes, doublings, half-doublings, strikes, slurs, shakes, tachum, cut notes, taorluath, throw, grip, birl) → tunes (Section 8, each prefaced by a "Be ready for:" skills list). Owner's critique (correct): it front-loads esoteric embellishments before tunes, which bores beginners. Photos live only in a scratchpad (HEIC), NOT committed. **Copyright:** the book is all-rights-reserved with an explicit no-copying/no-reformatting notice — so we use its *facts and methods* only, re-expressed originally; never its text, exact exercise sequences, or scans. Bogart is local; asking permission (via the La Crosse pipe band) is the clean path if we ever want to lift more directly. **Built this session:** (a) `reference/triad-method.md` — our own summary of the transition-drill method + the copyright note; (b) `src/triads.ts` — a **Finger Gym** of 11 note-to-note transition drills (`TRIADS`) generated from `chanter.ts`, ordered bottom-hand → top-hand → crossings/leaps, each with a **TAP/SNAP movement cue** computed from the fingering diff; (c) threaded an optional per-note `cue` through `tunes.ts`→`rhythmEngine.ts`→`RhythmLane.tsx` (shows under the diagram during Feel-the-pulse); (d) Play tab picker now grouped into "Tunes & patterns" + "Finger gym" (`ExerciseGroup` in `App.tsx`); (e) new Guide stage `finger-gym` in Phase 4 with CTAs into the drills; (f) `PEDAGOGY.md` §3a + the "Be ready for:" prereq proposal. **NOT verified in-app: this machine has no Node/npm installed (fresh clone), so `tsc`/build/Playwright couldn't be run here — changes were checked by hand only.** Verify (or install Node) before trusting the build.
- **Local toolchain set up + Finger Gym verified (2026-07-16):** installed Node v24.18.0 (winget), ran `npm install`, and added Playwright+Chromium as a devDependency (owner chose to keep it) — so build/lint/dev/browser-tests all run on this machine now. Note: Node isn't on a fresh shell's PATH; prepend Machine+User PATH in PowerShell first. The Finger Gym work now passes `npm run build` (tsc + vite), `oxlint` clean, and a Playwright check (drills render, D↔E cue reads "Snap up your top ring and pinky, and tap down your bottom index, bottom middle, and bottom ring" — correct). `package.json`/`package-lock.json` differ from origin only by Playwright; uncommitted.
- **Embellishment foundation built (2026-07-16):** the "grace notes" layer. `src/ornaments.ts` models an ornament as a flam of grace notes on the front of a melody note (`ExerciseNote.graces?: string[]`), threaded through `rhythmEngine.ts` (`TimedNote.graces`/`graceFreqs`) into both the lane and staff. Audio: `playOrnamentedNote(graceFreqs, principalFreq)` in `chanter.ts` (grace flam → principal). Rendering: the RhythmLane draws small satellite grace dots + a "grace: …" side label; StaffPlayer draws small gold grace noteheads with tiny stems before the principal (verified: 8 High-G graces above 8 D principals, correctly placed). **Only the high-G gracenote is shipped as solid** (the certain, curriculum-first ornament — a top-index flick sounding High G, on Low A–F); drills `orn-hg-<note>` in a new "Grace notes" Play group. **Doublings are deliberately NOT hardcoded** (project rule: don't guess piping specifics) — one flagged `orn-doubling-b` DRAFT exercises the multi-grace rendering, and `reference/embellishments.md` now has a doublings table left `[needs owner]`. Grace-notes tab rewritten from the coming-soon stub into a real landing (intro + CTAs into the drills); new Guide stage `first-grace` (new Phase 5 "Your first ornaments"). Verified in-browser, no console errors.
- **Multi-lens UX/pedagogy review + P1 fixes (2026-07-16):** ran a 4-lens review of the app (beginner, impatient, musician-new-to-pipes, skeptical pipe-major). Two ran as independent Sonnet subagents driving the live app (beginner + pipe-major both completed with strong reports); the other two hit the **session limit** (resets 6:20pm Central — background agents fail silently to 0-byte transcripts when the limit is hit, so trust the completion *notification*, not file size) and were role-played on the main thread. Full writeup saved to `reference/ux-review-2026-07.md`. **Headline:** foundations strong (pipe-major signed off on fingering/gracenote accuracy; I resolved his one staff-position doubt — it's standard treble clef, `staff.ts` is correct). **#1 issue (P0, not yet done):** the "Reading the music" Guide block (stages 8–10) dumps ~18 undefined terms before the learner has felt a beat — 3 of 4 lenses broke there; recommended resequencing it to after "Steady rhythm" + cutting tune-genre vocab + a musician fast-path. **P1 fixes applied + verified (build/lint/Playwright):** stripped dev-facing "draft/owner" wording from user-visible copy (Amazing Grace name/desc, Doubling of B name/desc, Grace-tab sentence, stage-15 concept) while keeping a learner-facing "provisional" note on the doubling; renamed `FINGER_NAMES[7]` 'pinky' → 'bottom pinky' in `triads.ts` (the D↔E cue no longer implies a nonexistent "top pinky"). **Deferred:** grace-note drills still show only the principal's fingering, not the grace flick (flash the grace shape during the flam); revisit the 85%/good×0.5 mastery curve; bring the first tune earlier.
- **P0 resequence done — "Reading the music" moved after "Play in time" (2026-07-16):** the review's #1 fix. Guide phase order is now Meet the instrument → The nine notes → Play in time (scale, finger gym, steady rhythm) → Reading the music (notation-why, staff, rhythm) → Your first tunes (read-along, Amazing Grace) → Your first ornaments → Onto the pipes — so the learner FEELS the beat before reading it. `curriculum.tsx` rewritten with the new order/phases; read-along + first-tune moved into a new "Your first tunes" phase. Also: cut the tune-genre jargon (strathspey/reel/jig/waltz/retreat) from `RhythmBasics`, defined the staff/clef/"ledger line" primitives plainly in `NotationBasics`/`StaffNotes` (were used undefined), added an "Already read music? skim this phase" fast-path line to the notation-why concept, and gave rhythm-reading a "you've already felt this pulse" callback. Verified: build + oxlint clean, Playwright confirmed the 7-phase order, reading-before-first-tune, fast-path text present, genre words gone, no console errors. **Not yet committed.**
- **Review follow-ups: motivation + grace pedagogy (2026-07-17):** worked the remaining implementable review items. (1) Added an "In a hurry? Hear a real tune ▸" shortcut on the Guide home header (`App.tsx` contents-head) that jumps into Play → Read mode of Amazing Grace — an early musical hook for impatient learners, per the review, without reordering the fundamentals-first curriculum. (2) Softened the mastery curve in `RhythmLane.tsx`: a "good" hit now weights `GOOD_WEIGHT = 0.75` toward accuracy (was 0.5), so an in-time-but-not-perfect run clears the 85% tempo-ladder gate — early wins feel attainable. (3) Grace-note drills now **flash the grace's fingering** on the diagram: on a non-miss hit of a note with graces, the ChanterDiagram briefly (200ms) shows the grace note's `covered` shape (e.g. the High-G top-index lift) before reverting — addresses the pipe-major's note that only the principal's fingering showed. Verified: build + oxlint clean, Playwright confirmed the shortcut routes to Read/Amazing Grace, the grace flash drops the diagram to High G's 4-covered shape mid-drill, no console errors. **Not yet committed.** Remaining review items are owner-blocked (Amazing Grace melody, doublings table) or done.
- **Feel-the-pulse vs Read-the-music — mic detection queued (2026-07-17):** owner observed the scored "Feel the pulse" falling-notes lane feels redundant now that "Read the music" has a moving-playhead staff. Key clarification surfaced in discussion: **Feel the pulse currently grades screen TAPS, not the mic / real chanter playing** — a proxy you could pass without touching the instrument (tap-mode was always scaffolding for the deferred mic version). Design read: the falling-notes lane is the better *timing* trainer (approach runway), the staff is the better *reading* trainer + more authentic for a piping audience; they train different skills, so the fix is framing, not deletion. What gives Feel the pulse a truly distinct identity is **mic-based onset detection** (grade real chanter timing) — the flagship deferred upgrade. **Decision: keep both Play modes AS-IS for now** (no merge/delete/reframe), and **mic-based onset detection is on the todo list as the intended resolution — not being built yet.** When picked up it dissolves the redundancy: Read = learn the dots, Feel = play your chanter and get graded for real. **(SUPERSEDED 2026-07-27 — Feel the pulse was removed entirely; the record/feedback engine now lands in the read-along Play surface, not a separate lane. See the restructure bullet below.)**
- **Big restructure — Play = read-along only, "Feel the pulse" removed (2026-07-27):** per owner, the app's real value is *reading real notation while playing along to adjustable audio*, so the tabs were sharpened to that spine (each tab now trains one thing). Changes: (a) **"Feel the pulse" (the tap-scored falling-notes lane) deleted** — it graded screen taps, not playing (a proxy you could pass without touching the chanter); `src/RhythmLane.tsx` removed (recoverable from git history — its timing-window + mastery-gate/tempo-ladder math is the reference for the *real* scoring engine). This **supersedes the 2026-07-17 "keep both modes" decision.** (b) **Play is now single-mode read-along** (`StaffPlayer` only): exercise picker (Tunes & patterns / Finger gym / Grace notes) + staff + moving playhead + manual tempo stepper. No modes, no scoring for now (owner OK'd the gap until the real feedback engine ships). (c) **"Explore notes" moved out of Play into the Scale tab** as a 2-way segmented toggle ("The scale" / "Explore notes"). (d) **Finger-gym movement `cue` now shows in `StaffPlayer`** (side panel, `.staffplay-cue`) so losing the lane didn't regress those drills — closes the old "wire cue into Read mode" todo. (e) nav: `PlayMode`→`ScaleMode` in `nav.ts`, `Preset.scaleMode` replaces `playMode`; all Guide/Embellishments CTAs rewired off the dead feel/notes modes; user-facing copy scrubbed of "Feel the pulse"/"tempo ladder"/"85%" auto-gate wording (`curriculum.tsx`, `content.tsx`, `App.tsx`, one `tunes.ts` description) — the manual tempo stepper stays but nothing auto-advances. Verified: `npm run build` (tsc+vite) + oxlint clean; Playwright confirmed all 4 tabs, Scale toggle + 9-note explorer, Play read-only (no segmented control, staff plays, count-in runs), Guide "In a hurry" shortcut → Play/Amazing Grace, a Guide CTA → Scale/Explore notes, Finger-gym cue renders ("Set the Low A position."), zero console errors. **Committed + pushed to `main` (fb8d3b6).**
- **Repeated-note fix + pitch/tuning control (2026-07-27):** (1) **Fixed an unplayable drill** — `steady-low-a` was eight separately-struck Low As, impossible on a chanter (the reed never stops; you can't re-strike a note without a gracenote). Now a single sustained Low A (`beats: 8`) with copy that teaches the why; the Guide "Steady rhythm" stage updated to match. Audited everything else: only that drill was wrong — the ornament drills already separate repeats with graces (correct), and Amazing Grace's draft + the scales + finger-gym have no plain repeats. **Authoring rule going forward: never put two consecutive identical notes in a plain (grace-less) exercise or tune.** Committed `97c35f5`. (2) **Built the pitch/tuning control** — the gap flagged earlier (tempo existed; pitch did NOT). A global tuning ratio in `chanter.ts` (`setTuningRatio`/`getTuningRatio` + `TUNING_DEFAULT/MIN/MAX_LOW_A`) scales every synthesized note at the source (`playChanterNote` + `scheduleReedTone`), so it's app-wide (Scale explorer, Play, ornaments); the metronome click stays unscaled. Surfaced as a `TuningControl` in Play (`App.tsx`): a Low-A-Hz slider (430–490, default 440) + Concert/Solo/Band presets, persisted in `localStorage` (`bagpipe-lab-tuning`) and applied on mount via a global state lifted into `App`. Intervals are preserved (the note freqs are the real A-Mixolydian bagpipe scale — chanter 'C'=C#, 'F'=F#, natural 'G'), so only the overall pitch level moves, never the notes or fingering. Verified: build + oxlint clean; a Playwright test hooked `createOscillator` and confirmed Low A sounds 440→480 after the Band preset **measured in the Scale explorer** (proving it's global), ratio 1.091, persists across reload, no console errors.
- **Mic-based "Tune to your chanter" — first mic feature (2026-07-27):** owner's point — nobody can drag a slider while both hands hold a Low A. So the tuning control now leads with a **"Tune to your chanter ♪"** button: tap it, sound a steady Low A, and the app listens (~2.2s) and sets its pitch to match. New `src/tuner.ts` — `detectChanterLowA(onLevel)` does `getUserMedia` → `AnalyserNode` → **banded autocorrelation** (searches only the ~415–492 Hz Low-A lag band, so it's immune to octave/harmonic errors) with parabolic interpolation for sub-Hz accuracy, then medians the good frames. The slider + Concert/Solo/Band presets stay as fine-tune / no-mic fallback; on permission denial or no clear tone it shows a friendly message and keeps the manual path. Tuning range widened to 415–500 Hz to fit detected values. **This is the first use of the mic — the plumbing the later onset/scoring engine builds on.** Two real bugs caught while verifying with a fake-audio Playwright test (a synthesized 472 Hz WAV fed via Chromium `--use-file-for-fake-audio-capture`): (a) the capture loop used `requestAnimationFrame`, which headless — and any backgrounded tab — throttles/stalls, so it's now a `setTimeout` loop; (b) a **StrictMode `useRef` "mounted" flag bug** — `activeRef` was only cleared on cleanup, and StrictMode's mount→cleanup→mount left it stuck `false`, so `tune()` bailed before updating the UI even though detection returned a perfect 472.01 Hz (fix: set the ref `true` on mount, not only false on cleanup). Verified end-to-end: detects 472 → sets Low A = 472, shows "Tuned!", persists, no console errors; build + oxlint clean. **iOS TODO:** the native WKWebView build needs `NSMicrophoneUsageDescription` in `Info.plist` (+ Capacitor mic-permission handling) before the tuner — or the later scoring engine — works on device; not needed for the web/Pages preview.
- **iOS hardening for TestFlight mic validation (2026-07-27):** the App Store is the destination. The owner's shipped **Jazz Guitar app** proves the whole stack — Apple account/LLC, RevenueCat one-time IAP accepting revenue, App Review clearance, Codemagic→TestFlight, and single-file React+Capacitor — so account/monetization/review are a reuse, not a risk. **But that app does NOT use the mic, so `getUserMedia` in WKWebView is entirely unproven in this stack**, and it's the foundation under the tuner + the whole feedback-engine roadmap. **Decision: validate the mic on a real device NOW, before building the scoring engine on top of it** (WKWebView ≠ the Chromium we've been verifying in; audio/mic behave differently, and this Windows box can't build iOS at all). Applied the code/config prereqs (all that's possible from Windows): (a) `ios/App/App/Info.plist` — added `NSMicrophoneUsageDescription`; (b) `ios/App/App/AppDelegate.swift` — set `AVAudioSession` to `.playAndRecord` + `.defaultToSpeaker` so audio plays through the silent switch and mic+speaker coexist routed to the speaker (not earpiece) — may need tuning after device testing. Safe-area was already handled (`.tab-bar` has `env(safe-area-inset-bottom)`; Capacitor insets the top). Validated `Info.plist` parses (plistlib). Guitar-app repo isn't checked out locally (`../Jazz Guitar App` not found), so used standard iOS config, not a mirror. **Owner's side (not doable on Windows):** run Codemagic → TestFlight → test on iPhone with a real chanter — does it render, does audio play with the **silent switch ON**, and **does the tuner detect a real Low A**. Mic works → feedback engine de-risked; mic doesn't → learned cheaply, before building on it.
- **Next concrete steps:** (0) **[needs owner]** the Amazing Grace plain melody as note names — `amazing-grace` in `tunes.ts` (used by Play + the "Your first tune" stage) is still the DRAFT D-tonic setting; and the doublings table in `reference/embellishments.md` so doublings can graduate from draft to a real drill. (1) **Record + timing-feedback engine** — the flagship, but **GATED on first confirming the mic works in WKWebView on a real device** (TestFlight; iOS prereqs committed — mic plist + `.playAndRecord` audio session): mic onset detection (build on `src/tuner.ts`'s `getUserMedia`→`AnalyserNode` plumbing + the `setTimeout` capture-loop pattern), record-then-analyze (not real-time), overlay per-note timing on the same reading staff; needs a latency-calibration step and Capacitor/iOS mic handling. Scope timing FIRST; note/pitch correctness harder, embellishment-cleanliness research-grade — defer both. Re-home the old lane's mastery-gate/tempo-ladder concept onto this real signal (git history has `RhythmLane.tsx` for the timing-window math). (2) **Embellishments ("Grace notes") tab → a real ornament-drilling surface** (owner's vision): drill each ornament with a slow "for your fingers" pass + an at-tempo "for your ear" pass; attach ornaments to Amazing Grace per high-G grace → D strike → light D throw → G catch once melody + doublings are confirmed. Keep it a separate section from Play; Play stays plain-first with ornaments layered in later. (3) `tunes.ts` still uses relative `beats`, not real note-values; `StaffPlayer` conveys duration only via horizontal spacing — give `tunes.ts` real note-value data if faithful engraving is ever needed. (4) Implement the "Be ready for:" tune-prereq checklists. (5) Optional: change the default synth pitch to real chanter level (~478–480 Hz), since real chanters run sharp of concert 440 — kept at 440 for now to avoid a disruptive baseline-sound change; the tuning control already lets users move there.
